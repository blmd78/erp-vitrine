/**
 * Core Logger class for NextNode Logger
 * Zero dependencies, strict TypeScript, production-ready logging with scope support
 */
import type { Logger, LogObject, LoggerConfig } from '../types.js';
export declare class NextNodeLogger implements Logger {
    private readonly environment;
    private readonly prefix?;
    private readonly includeLocation;
    constructor(config?: LoggerConfig);
    private extractScope;
    private createLogEntry;
    private output;
    private log;
    info(message: string, object?: LogObject): void;
    warn(message: string, object?: LogObject): void;
    error(message: string, object?: LogObject): void;
}
export declare const createLogger: (config?: LoggerConfig) => Logger;
export declare const logger: Logger;
//# sourceMappingURL=logger.d.ts.map