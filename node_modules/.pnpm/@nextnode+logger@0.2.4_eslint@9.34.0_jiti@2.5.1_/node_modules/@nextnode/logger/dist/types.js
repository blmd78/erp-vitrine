/**
 * Core types for NextNode Logger
 * Zero dependencies, strict TypeScript, production-ready logging
 */
// Type guards for runtime type checking
export const isLogLevel = (value) => typeof value === 'string' && ['info', 'warn', 'error'].includes(value);
export const isEnvironment = (value) => typeof value === 'string' && ['development', 'production'].includes(value);
export const isDevelopmentLocation = (location) => 'file' in location && 'line' in location;
export const isRuntimeEnvironment = (value) => typeof value === 'string' &&
    ['node', 'browser', 'webworker', 'unknown'].includes(value);
//# sourceMappingURL=types.js.map