/**
 * Image optimization library types
 * Supports modern image formats and optimization strategies
 */
/**
 * Supported image formats
 */
export type ImageFormat = 'svg' | 'png' | 'webp' | 'avif' | 'jpeg' | 'jpg';
/**
 * Security limits to prevent DoS attacks
 * Protects against memory exhaustion and resource abuse
 */
export declare const SECURITY_LIMITS: {
    /** Maximum image file size: 50MB */
    readonly MAX_FILE_SIZE: number;
    /** Maximum image width: 10000px */
    readonly MAX_WIDTH: 10000;
    /** Maximum image height: 10000px */
    readonly MAX_HEIGHT: 10000;
    /**
     * Maximum total pixels (width * height): 50 mebipixels
     * Prevents MAX_WIDTH × MAX_HEIGHT (100M pixels) from being allowed
     * Individual dimensions can be at max, but not both simultaneously
     */
    readonly MAX_PIXELS: number;
};
/**
 * Optimized image result
 */
export interface OptimizedImage {
    /** Optimized image source URL */
    src: string;
    /** Image width in pixels */
    width: number;
    /** Image height in pixels */
    height: number;
    /** Output format */
    format: ImageFormat;
    /** Optional srcset for responsive images */
    srcSet?: string;
    /** Optional sizes attribute */
    sizes?: string;
}
/**
 * Quality configuration per image format
 * Based on 2025 best practices:
 * - PNG: 100% (lossless)
 * - WebP: 90% (optimal balance)
 * - AVIF: 85% (better compression)
 * - JPEG: 80% (standard web quality)
 */
export interface ImageQualityConfig {
    png: number;
    webp: number;
    avif: number;
    jpeg: number;
    jpg: number;
}
/**
 * Compression profile types
 */
export type CompressionProfile = 'lossless' | 'balanced' | 'aggressive';
/**
 * Responsive image configuration
 */
export interface ResponsiveImageConfig {
    /** Breakpoint widths in pixels (default: [640, 1024, 1920, 2560]) */
    breakpoints: number[];
    /** Pixel densities for high-DPI displays (default: [1, 2, 3]) */
    densities: number[];
    /** Sizes attribute value for responsive images */
    sizes?: string;
    /** Maximum width to generate */
    maxWidth?: number;
}
/**
 * LQIP (Low Quality Image Placeholder) configuration
 */
export interface LQIPConfig {
    /** Enable LQIP generation */
    enabled: boolean;
    /** LQIP dimensions (default: 20x20) */
    width: number;
    height: number;
    /** LQIP quality (default: 10) */
    quality: number;
    /** Apply blur filter */
    blur: number;
    /** Output format for LQIP */
    format: 'webp' | 'jpeg';
}
/**
 * Image optimization options
 */
export interface ImageOptimizationOptions {
    /** Target format (auto-detect if not specified) */
    format?: ImageFormat;
    /** Quality (0-100) - uses defaults if not specified */
    quality?: number;
    /** Target width for resizing */
    width?: number;
    /** Target height for resizing */
    height?: number;
    /** Responsive image configuration */
    responsive?: Partial<ResponsiveImageConfig>;
    /** LQIP configuration */
    lqip?: Partial<LQIPConfig>;
    /** Compression profile */
    profile?: CompressionProfile;
}
/**
 * Image validation constraints
 */
export interface ImageValidationConfig {
    /** Maximum file size in bytes */
    maxFileSize?: number;
    /** Maximum width in pixels */
    maxWidth?: number;
    /** Maximum height in pixels */
    maxHeight?: number;
    /** Minimum width in pixels */
    minWidth?: number;
    /** Minimum height in pixels */
    minHeight?: number;
    /** Allowed formats */
    allowedFormats?: ImageFormat[];
}
/**
 * Image validation result
 */
export interface ImageValidationResult {
    valid: boolean;
    errors: string[];
}
/**
 * Cache strategy configuration
 */
export interface CacheStrategyConfig {
    /** Cache duration in seconds (default: 31536000 = 1 year) */
    maxAge: number;
    /** Immutable flag for content-addressed assets */
    immutable: boolean;
    /** CDN-specific headers */
    cdnHeaders?: Record<string, string>;
}
/**
 * Generic image source input
 * Supports file paths, URLs, or buffer data
 */
export type ImageSource = string | Buffer | {
    src: string;
    format?: ImageFormat;
};
/**
 * Batch optimization result
 */
export interface BatchOptimizationResult<T = ImageSource> {
    /** Successfully optimized images */
    successful: Array<{
        input: T;
        output: OptimizedImage;
    }>;
    /** Failed optimizations with error messages */
    failed: Array<{
        input: T;
        error: string;
    }>;
}
//# sourceMappingURL=types.d.ts.map