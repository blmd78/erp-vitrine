/**
 * Responsive image utilities for srcset and sizes generation
 * Based on 2025 best practices for responsive images
 */
import type { OptimizedImage, ResponsiveImageConfig } from '../types.js';
/**
 * Standard responsive breakpoints (2025 best practices)
 * - 640px: Smartphones
 * - 1024px: Tablets
 * - 1920px: Desktop
 * - 2560px: Large displays (Retina)
 */
export declare const DEFAULT_BREAKPOINTS: number[];
/**
 * Standard pixel densities for high-DPI displays
 */
export declare const DEFAULT_DENSITIES: number[];
/**
 * Default responsive image configuration
 */
export declare const DEFAULT_RESPONSIVE_CONFIG: ResponsiveImageConfig;
/**
 * Generate srcset string from array of optimized images
 *
 * @example
 * ```typescript
 * const srcset = generateSrcSet([
 *   { src: '/img-640.webp', width: 640, height: 480, format: 'webp' },
 *   { src: '/img-1024.webp', width: 1024, height: 768, format: 'webp' }
 * ])
 * // Returns: "/img-640.webp 640w, /img-1024.webp 1024w"
 * ```
 */
export declare function generateSrcSet(images: OptimizedImage[]): string;
/**
 * Generate sizes attribute for responsive images
 * Uses mobile-first approach with max-width
 *
 * @example
 * ```typescript
 * generateSizes([
 *   { breakpoint: 640, size: '100vw' },
 *   { breakpoint: 1024, size: '50vw' }
 * ])
 * // Returns: "(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 50vw"
 * ```
 */
export declare function generateSizes(sizes: Array<{
    breakpoint: number;
    size: string;
}>, defaultSize?: string): string;
/**
 * Calculate responsive widths based on breakpoints and densities
 */
export declare function calculateResponsiveWidths(breakpoints: number[], densities: number[], maxWidth?: number): number[];
/**
 * Calculate image dimensions maintaining aspect ratio
 */
export declare function calculateImageDimensions(originalWidth: number, originalHeight: number, targetWidth?: number, targetHeight?: number): {
    width: number;
    height: number;
};
/**
 * Get standard responsive breakpoints with optional custom breakpoints
 */
export declare function getResponsiveBreakpoints(customBreakpoints?: number[]): number[];
/**
 * Generate common sizes attribute patterns
 */
export declare const COMMON_SIZES: {
    /** Full width on all screens */
    readonly fullWidth: "100vw";
    /** Half width on desktop, full on mobile */
    readonly halfDesktop: "(max-width: 768px) 100vw, 50vw";
    /** Two columns on desktop, full on mobile */
    readonly twoColumns: "(max-width: 768px) 100vw, 50vw";
    /** Three columns on desktop, two on tablet, full on mobile */
    readonly threeColumns: "(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 33.33vw";
    /** Hero image - full width with max-width constraint */
    readonly hero: "(max-width: 1920px) 100vw, 1920px";
    /** Content image - constrained width */
    readonly content: "(max-width: 768px) 100vw, 768px";
    /** Thumbnail - fixed small size */
    readonly thumbnail: "(max-width: 640px) 150px, 300px";
};
/**
 * Build picture element sources for art direction
 * Different crops/images for different screen sizes
 */
export interface PictureSource {
    media: string;
    srcSet: string;
    type?: string;
}
export declare function buildPictureSources(sources: Array<{
    breakpoint: number;
    images: OptimizedImage[];
}>): PictureSource[];
//# sourceMappingURL=responsive.d.ts.map