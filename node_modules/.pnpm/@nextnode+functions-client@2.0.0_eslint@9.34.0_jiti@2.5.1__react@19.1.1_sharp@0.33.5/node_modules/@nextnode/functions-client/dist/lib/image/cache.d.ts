/**
 * Cache strategy configuration for optimized images
 * Based on best practices for CDN and browser caching
 */
import type { CacheStrategyConfig } from './types.js';
/**
 * Default cache strategy for optimized images
 * - 1 year cache duration (31536000 seconds)
 * - Immutable flag for content-addressed assets
 */
export declare const DEFAULT_CACHE_STRATEGY: CacheStrategyConfig;
/**
 * Cache durations in seconds
 */
export declare const CACHE_DURATIONS: {
    /** 1 year - for immutable content-addressed assets */
    readonly ONE_YEAR: 31536000;
    /** 1 month - for semi-static content */
    readonly ONE_MONTH: 2592000;
    /** 1 week - for frequently updated content */
    readonly ONE_WEEK: 604800;
    /** 1 day - for dynamic content */
    readonly ONE_DAY: 86400;
    /** 1 hour - for very dynamic content */
    readonly ONE_HOUR: 3600;
};
/**
 * Generate Cache-Control header value
 */
export declare function getCacheControlHeader(config: CacheStrategyConfig): string;
/**
 * CDN-specific headers for popular CDN providers
 */
export declare const CDN_HEADERS: {
    /** Cloudflare cache headers */
    readonly cloudflare: {
        readonly 'CF-Cache-Status': "HIT";
        readonly 'CDN-Cache-Control': "max-age=31536000, immutable";
    };
    /** Fastly cache headers */
    readonly fastly: {
        readonly 'Surrogate-Control': "max-age=31536000, immutable";
        readonly 'Fastly-IO-Info': "img-processed";
    };
    /** AWS CloudFront headers */
    readonly cloudfront: {
        readonly 'CloudFront-Is-Desktop-Viewer': "true";
        readonly 'CloudFront-Viewer-Country': "*";
    };
    /** Generic CDN headers */
    readonly generic: {
        readonly 'X-Cache': "HIT";
        readonly Age: "0";
    };
};
/**
 * Get complete cache headers for a CDN provider
 */
export declare function getCDNHeaders(provider: keyof typeof CDN_HEADERS, cacheStrategy?: CacheStrategyConfig): Record<string, string>;
/**
 * Content-addressed naming pattern
 * Ensures unique URLs for different versions
 */
export declare function generateContentAddressedName(originalName: string, hash: string): string;
/**
 * Check if asset is content-addressed
 * Content-addressed assets have a hash in the filename
 */
export declare function isContentAddressed(filename: string): boolean;
/**
 * Determine appropriate cache strategy based on asset type
 */
export declare function getCacheStrategyForAsset(filename: string, isOptimized: boolean): CacheStrategyConfig;
/**
 * Validate cache hash format
 * Ensures hash is a valid SHA-256 hex string (64 characters)
 * Prevents cache poisoning attacks through invalid hashes
 */
export declare function validateCacheHash(hash: string): boolean;
/**
 * Sanitize filename to prevent directory traversal and injection
 * Removes dangerous characters while preserving valid filename structure
 * Handles multi-byte characters (emoji) and preserves extension when truncating
 */
export declare function sanitizeFilename(filename: string): string;
/**
 * Generate secure cache key from filename and hash
 * Validates inputs to prevent cache poisoning
 */
export declare function generateSecureCacheKey(filename: string, hash: string): string;
//# sourceMappingURL=cache.d.ts.map