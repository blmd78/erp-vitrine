/**
 * Image optimization library types
 * Supports modern image formats and optimization strategies
 */
/**
 * Security limits to prevent DoS attacks
 * Protects against memory exhaustion and resource abuse
 */
export const SECURITY_LIMITS = {
    /** Maximum image file size: 50MB */
    MAX_FILE_SIZE: 50 * 1024 * 1024,
    /** Maximum image width: 10000px */
    MAX_WIDTH: 10000,
    /** Maximum image height: 10000px */
    MAX_HEIGHT: 10000,
    /**
     * Maximum total pixels (width * height): 50 mebipixels
     * Prevents MAX_WIDTH × MAX_HEIGHT (100M pixels) from being allowed
     * Individual dimensions can be at max, but not both simultaneously
     */
    MAX_PIXELS: 50 * 1024 * 1024,
};
//# sourceMappingURL=types.js.map