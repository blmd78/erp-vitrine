/**
 * Image format detection and validation utilities
 */
import type { ImageFormat, ImageValidationConfig, ImageValidationResult } from '../types.js';
/**
 * Detect image format from file path or MIME type
 */
export declare function detectImageFormat(source: string, mimeType?: string): ImageFormat | null;
/**
 * Check if format is a vector format (no raster processing needed)
 */
export declare function isVectorFormat(format: ImageFormat): boolean;
/**
 * Check if format supports transparency
 */
export declare function supportsTransparency(format: ImageFormat): boolean;
/**
 * Get recommended format based on image characteristics
 */
export declare function getRecommendedFormat(options: {
    hasTransparency?: boolean;
    isPhoto?: boolean;
    isIcon?: boolean;
}): ImageFormat;
/**
 * Validate image dimensions
 */
export declare function validateImageDimensions(width: number, height: number, config: ImageValidationConfig): ImageValidationResult;
/**
 * Validate image file size
 */
export declare function validateImageSize(sizeInBytes: number, config: ImageValidationConfig): ImageValidationResult;
/**
 * Validate image format
 */
export declare function validateImageFormat(format: ImageFormat, config: ImageValidationConfig): ImageValidationResult;
/**
 * Comprehensive image validation
 */
export declare function validateImage(format: ImageFormat, width: number, height: number, sizeInBytes: number, config: ImageValidationConfig): ImageValidationResult;
//# sourceMappingURL=format.d.ts.map