/**
 * Image quality presets and compression profiles
 * Based on 2025 best practices for optimal file size vs quality balance
 */
import type { CompressionProfile, ImageFormat, ImageQualityConfig } from '../types.js';
/**
 * Default quality settings per format
 * - PNG: 100% (lossless compression)
 * - WebP: 90% (optimal balance for web)
 * - AVIF: 85% (better compression than WebP)
 * - JPEG: 80% (standard web quality)
 */
export declare const DEFAULT_QUALITY: ImageQualityConfig;
/**
 * Lossless compression profile
 * Maximum quality, larger file sizes
 */
export declare const LOSSLESS_QUALITY: ImageQualityConfig;
/**
 * Aggressive compression profile
 * Smaller file sizes, acceptable quality loss
 */
export declare const AGGRESSIVE_QUALITY: ImageQualityConfig;
/**
 * Get quality configuration for a compression profile
 */
export declare function getQualityProfile(profile: CompressionProfile): ImageQualityConfig;
/**
 * Get quality value for a specific format and profile
 */
export declare function getQualityForFormat(format: ImageFormat, profile?: CompressionProfile, customQuality?: number): number;
/**
 * Determine if format should use mozjpeg optimization
 */
export declare function shouldUseMozjpeg(format: ImageFormat): boolean;
/**
 * Determine if format should use pngquant optimization
 */
export declare function shouldUsePngquant(format: ImageFormat): boolean;
/**
 * Get Sharp compression options for a format
 */
export declare function getCompressionOptions(format: ImageFormat, quality: number): Record<string, unknown>;
//# sourceMappingURL=quality.d.ts.map