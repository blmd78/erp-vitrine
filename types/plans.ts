/**
 * Unified plan types
 * Single source of truth for all pricing plan identifiers across the application
 */

/**
 * Available pricing plans
 * Used for both UI theming and i18n translations
 */
export type Plan = 'starter' | 'business' | 'enterprise'
